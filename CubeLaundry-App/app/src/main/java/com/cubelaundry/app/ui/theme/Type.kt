package com.cubelaundry.app.ui.theme

import androidx.compose.ui.text.font.FontFamily

// Using the system default font for now. To use Space Grotesk / Inter from
// Google Fonts instead, add the "androidx.compose.ui:ui-text-google-fonts"
// dependency and a res/values/arrays.xml with the Google Fonts certificate
// array, then switch back to the GoogleFont.Provider approach.
val SpaceGrotesk = FontFamily.Default
val Inter = FontFamily.Default
